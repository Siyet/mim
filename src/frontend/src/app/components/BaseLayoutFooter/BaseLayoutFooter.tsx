import React, {PureComponent} from 'react';

import './BaseLayoutFooter.less';

class BaseLayoutFooter extends PureComponent<{}, {}> {
  state = {
  
  };

  render() {
    return(
      <div></div>
    );
  }
}

export {
  BaseLayoutFooter,
};
